#include<cstdio>
#include<cstring>
#include<algorithm>
using namespace std;
int main()
{
	char s[20];
	while(scanf("%s",s)==1)
	{
		int l=strlen(s);
//		printf("%d",l,"/n"); 
		sort(s,s+l);
		do
		{
			puts(s);
		}while(next_permutation(s,s+l));//c++函数
	}
}
